# ═══════════════════════════════════════════════════════════
# application.properties — DriveAway Backend Configuration
#
# IMPORTANT NOTES about your pom.xml:
#   - You are using Spring Boot 4.0.3 (very new)
#   - You use spring-boot-starter-webmvc (not spring-boot-starter-web)
#   - You have spring-boot-starter-security enabled
#   - MongoDB is configured in MongoConfig.java (hardcoded URL)
#     so spring.data.mongodb.uri here is a backup only
# ═══════════════════════════════════════════════════════════

# ── Server ────────────────────────────────────────────────
server.port=8080

# ── App name ──────────────────────────────────────────────
spring.application.name=driveaway

# ── MongoDB ───────────────────────────────────────────────
# Your MongoConfig.java hardcodes the URL, so this is backup.
# Database name MUST be driveaway_db to match your Atlas collections.
spring.mongodb.database.uri=mongodb+srv://DriveAway_01:driveaway%401@driveaway.rm74yrq.mongodb.net/driveaway_db?retryWrites=true&w=majority
spring.mongodb.database=driveaway_db

# ── Jackson: serialize LocalDate as "2024-12-01" not [2024,12,1] ──
#spring.jackson.serialization.write-dates-as-timestamps=false

# ── Logging ───────────────────────────────────────────────
logging.level.com.driveaway=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG

# ── Security: disable default login page (you have your own) ──
# Your SecurityConfig.java already handles this, but just in case:
spring.security.user.name=admin
spring.security.user.password=admin123